version: 2.1

jobs:
  build:
    docker:
      - image: busybox

    steps:
      - run: echo build

  test:
    docker:
      - image: busybox

    steps:
      - run: echo test

  deploy:
    docker:
      - image: busybox

    steps:
      - run: echo deploy

workflows:
  version: 2
  build-test-and-approval-deploy:
    jobs:
      - build
      - test:
          requires:
            - build
      #- hold:
      #    type: approval
      #    requires:
      #     - test
      - deploy:
          type: approval
          requires:
            - build
